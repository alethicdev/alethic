services:
  alethic-api:
    build:
      context: .
      target: api
    ports:
      - "8000:8000"
    environment:
      - ALETHIC_STORE=memory

  ollama:
    image: ollama/ollama
    ports:
      - "11434:11434"
    profiles:
      - llm
